import { DatePassedPipe } from './date-passed.pipe';

describe('DatePassedPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePassedPipe();
    expect(pipe).toBeTruthy();
  });
});
